
  <div class="main-content container">
    <% if notice %>
      <div class="alert alert-success" role="alert"><%= notice %></div>
    <% end %>
    <div class="ms-2 mt-2">
    </div>
      <div class="d-lg-flex justify-content-between align-items-center gap-3 me-5">
       <div class="d-flex gap-3 align-items-center justify-content-start">
         <%= link_to "Back", admin_app_offers_path,class:"btn btn-dark fw-medium" %>
        <h1 class="edit-heading">Editing app offer</h1>
       </div>
        <a href="/admin/app_offers/<%= @app_offer.id %>/edit/event/new" class="btn btn-success fw-medium me-5 w-25">New Event</a>
    </div>
    <div class="d-flex justify-content-between align-items-center">
      <%= render "form", app_offer: @app_offer %>
    </div>
  </div>

