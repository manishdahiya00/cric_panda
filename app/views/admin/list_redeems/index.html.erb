    <% if notice %>
      <div class="alert alert-success" role="alert"><%= notice %></div>
    <% end %>
        <% if alert %>
      <div class="alert alert-danger" role="alert"><%= alert %></div>
    <% end %>
     <div class="d-flex justify-content-between align-items-center">
      <h1 class="mb-4">Listing Redeems</h1>
    </div>
      <div>
    <%= form_with(url: admin_list_redeems_path, method: :get) do |f| %>
      <div class="form-group">
        <%= f.text_field :paytm_number, placeholder: "Search by paytm_number", class: "form-control" %>
      </div>
    <% end %>
  </div>
<%= render "list_redeem" %>
<%= paginate @app_redeems %>
