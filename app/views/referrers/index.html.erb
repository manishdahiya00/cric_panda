
  <div class="main-content">
    <% if notice %>
      <div class="alert alert-danger" role="alert"><%= notice %></div>
    <% end %>
    <div class="d-flex justify-content-between align-items-center">
      <h1 class="mb-4">Listing Referrers</h1>
    </div>
    <div>
             <div>
    <%= form_with(url: referrers_path, method: :get) do |f| %>
      <div class="form-group">
        <%= f.text_field :paytm_number, placeholder: "Search by paytm_number", class: "form-control" %>
      </div>
    <% end %>
  </div>
      <%= render "referrer" %>
      <%= paginate @referrers %>
    </div>
  </div>
